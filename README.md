# Одностраничный сайт для салона красоты (парикмахерской)

## **Barbershop Kety**

Barbershop_Kety — это лендинг для парикмахерской, который показывает информацию об услугах и мастерах, принимает отзывы и отображает медиа.

Бэкенд реализован на **Django** (шаблоны, маршруты, модели и опционально API через Django REST Framework), фронтенд — классические **HTML/CSS/JS** в шаблонах и static-папках. Для разработки используется локальная база данных **SQLite**, а для хранения загружаемых файлов — папка media. Взаимодействие клиентской части с сервером через обычную отправку форм или через Fetch/AJAX к API-эндпоинтам. Демонстрационные изображения взяты из свободных источников и используются только для показа внешнего вида и функционала сайта.

---

### **Функционал**

В приложении реализовано следующее:

- При нажатии на пункты меню страница плавно прокручивается к соответствующему разделу, при этом отображается кнопка «Наверх» в виде стрелки при прокрутке вниз.
- В разделе "Услуги и цены" реализовано отображение прайс-листа и изображения-обложки услуги в зависимости от выбранной услуги. Услуги, цены и изображение-обложку можно добавлять/изменять/удалять из админ панели Django.
- В разделе "О нас" реализован слайдер карточек, содержащих данные о мастерах (имя, специализация, краткое описание, способы связи). Карточка представляет собой фото мастера, а при нажатии на нее появляется информация о мастере, при этом иконки способов связи при наведении меняют цвет. Мастеров и их данные можно добавлять/изменять/удалять из админ панели Django.
- В разделе "Галерея работ мастеров" представлены фото в миниатюре при наведении на них фото выделяется, а при нажатии открывается модальное окно с увеличенным фото.
- В разделе "Оставьте свой отзыв" размещена форма для создания отзыва с оценкой стилизованной под звезды, также отзывы отображаются в виде слайдера с автопрокруткой. После отправки отзыва его нужно опубликовать на сайте через админ панель Django.
- В разделе "Контакты" представлена контактная информация и в том числе карта с местоположением компании. Email, телефон, часы работы, адрес и координаты для карты можно задавать через админ панель Django.

---

### **Требования**

Убедитесь, что в вашей системе установлены Python и Node.js:

- [Python version 3.13.1+](https://www.python.org/downloads/)
- [Node version 22.12.0+](https://nodejs.org/en/download/)

Проверьте версии командой:

- ```python --version```
- ```node --version```
- ```npm --version```

---

### **Структура проекта**

**Общая корневая структура репозитория:**

- 📁 `BarbershopKety/` – корневая папка проекта (репозиторий).
  - 📁 `assets/` – вспомогательные медиа-файлы (анимации).
    - `Animationofwork.gif` – GIF-анимация/демо.
    - `Animationofwork.mp4` – видео-анимация/демо.
  - 📁 `barbershop/` – основной Django-пакет (settings и конфигурация проекта).
  - 📁 `landing/` – Django-приложение, реализующее лендинг сайта (шаблоны, статические файлы, модели, вьюхи).
  - 📁 `media/` – папка для загружаемых пользователями файлов (сейчас там находятся изображения для демонстрации сайта).
  - 📄 `.gitignore` – правила игнорирования файлов Git.
  - 📄 `db.sqlite3` – файл локальной базы данных SQLite.
  - 📄 `LICENSE` – лицензия проекта.
  - 📄 `manage.py` – утилита для управления проектом (миграции, запуск сервера и т.д.).
  - 📄 `READMY.md` – README (описание проекта, инструкция по запуску).
  - 📄 `requirements.txt` – список зависимостей Python-проекта.

**Подробно: Django-проект (каркас):**

- 📁 `barbershop/` – корневой пакет Django-проекта (информация о конфигурации).
  - 📄 `asgi.py` – точка входа для ASGI-сервера (используется при развёртывании с асинхронной поддержкой).
  - 📄 `wsgi.py` – точка входа для WSGI-сервера (Gunicorn, uWSGI) при синхронном развёртывании.
  - 📄 `settings.py` – файл настроек проекта (базы данных, INSTALLEDAPPS, STATIC/MEDIA, middleware, секретный ключ и пр.).
  - 📄 `urls.py` – глобальные маршруты (root URLConf), обычно подключает `landing.urls` и админку.

**Подробно: Django-приложение landing:**

- 📁 `landing/` – Django-приложение, реализующее публичную (лендинг) часть сайта.
  - 📁 `migrations/` – автогенерируемые миграции базы данных для моделей приложениия.
  - 📁 `static/landing/` – статические файлы приложения (CSS, JS, шрифты, изображения).
    - 📁 `css/` – таблицы стилей.
    - 📁 `fonts/` – шрифты, используемые на сайте.
    - 📁 `images/` – изображения для шаблонов.
    - 📁 `js/` – JavaScript-код, отвечающий за взаимодействие на клиенте.
  - 📁 `templates/landing/` – шаблоны Django для этого приложения.
    - 📁 `sections/` – отдельные фрагменты шаблонов, которые инклюдятся в страницы.
    - 📄 `base.html` – базовый шаблон (общая разметка, подключение CSS/JS, блоки для наследования).
    - 📄 `index.html` – основная страница лендинга, наследует `base.html`.
  - 📄 `admin.py` – регистрация моделей в админке Django.
  - 📄 `apps.py` – конфигурация приложения (AppConfig), тут прописано имя приложения.
  - 📄 `models.py` – определение моделей (ORM) приложения.
  - 📄 `serializers.py` – используется для API, описывает, какие поля моделей сериализуются в JSON/из JSON.
  - 📄 `tests.py` – модуль для unit-тестов приложения (используйте pytest или стандартный TestCase).
  - 📄 `urls.py` – роуты, специфичные для приложения.
  - 📄 `views.py` – обработчики HTTP-запросов (функциональные или классовые вьюхи).

---

### **Локальный запуск приложения**

1. Запускаем терминал в директории, где планируется разместить папку с файлами проекта
2. Клонируем репозиторий:\
   `git clone https://github.com/AntistesEM/Barbershop_Kety.git`
3. Открываем папку `Barbershop_Kety` в любой IDE.
4. Запускаем приложение:

      -- запускаем встроенный терминал в папке `Barbershop_Kety`

      -- создаём виртуальное окружение:

      ```bash
      python -m venv venv
      ```

      -- активируем виртуальное окружение:

      ```bash
      .\venv\Scripts\activate
      ```

      -- устанавливаем зависимости:

      ```bash
      pip install -r requirements.txt
      ```

      -- запускаем сервер:

      ```bash
      python manage.py runserver
      ```

    После этого по ссылке [127.0.0.1:8000](http://127.0.0.1:8000/) будет доступна главная страница приложения, а по ссылке [127.0.0.1:8000/admin/](http://127.0.0.1:8000/admin/) будет доступна админ панель Django (логин: admin, пароль: admin).

---

### **Используемые технологии**

#### Backend

- **Язык программирования**: Python
- **Веб‑фреймворк**: Django — маршруты, модели, шаблоны и админ‑панель
- **API**: Django REST Framework (DRF) — для JSON‑эндпоинтов
- **База данных**: SQLite
- **Сервер приложений**: Django Development Server

#### Frontend

- **Язык программирования**: JavaScript
- **Работа с API**: Fetch API (для AJAX запросов)
- **Управление DOM и событиями**: DOM Events
- **Стили**: CSS
- **Вёрстка**: HTML5, CSS3
- **Библиотеки и плагины**:
  - Slick — слайдеры/карусели
  - SimpleBar — кастомные скроллбары
  - Leaflet — интерактивная карта (альтернатива Google Maps, без API-ключа)
  - Font Awesome (CDN) — иконки
  - jQuery — для плагинов, зависящих от него

---

<p align="center">
    <img src="assets/Animation_of_work.gif" alt="Анимация работы приложения" width="800" />
</p>
